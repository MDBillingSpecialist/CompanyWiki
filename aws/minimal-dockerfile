FROM node:18-alpine

# Create app directory
WORKDIR /app

# Install Express
RUN npm install express --quiet

# Copy server file
COPY ./aws/minimal-express-server.js .

# Expose port
EXPOSE 3000

# Set environment variables
ENV NODE_ENV=production
ENV PORT=3000

# Start server
CMD ["node", "minimal-express-server.js"]